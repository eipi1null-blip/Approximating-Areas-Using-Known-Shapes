# Area Approximation Using Known Shapes

Standalone GitHub Pages applet for approximating the area of an irregular region with grid squares. Users can adjust the region, change grid size, and compare inner and outer approximations.
